## 1. Shared focus helper and hook

- [x] 1.1 Add `isTextEntryTarget(el: Element | null): boolean` to `packages/app/src/platform/shell/` (new `focusTargets.ts`, or exported from the hook file): returns true when `el` is an `<input>`, `<textarea>`, `<select>`, has `isContentEditable`, or matches `el.closest(".cm-editor")`. Export it; do NOT retrofit the nine existing ad-hoc copies (`useShortcuts.ts:30-36`, `useCommandHotkeys.ts:19-24`, `useSaveShortcut.ts:37`, `postgres/data/TableViewerTab.tsx:555-561`, etc.) — their guards differ deliberately.
- [x] 1.2 Create `packages/app/src/platform/shell/useAutoFocusOnActivate.ts` exporting `useAutoFocusOnActivate({ active, rootRef, targetRef })` where `targetRef: RefObject<{ focus(): void } | null>`. Mirror the file/doc-comment style of the neighbouring `useSaveShortcut.ts` and reference issue #280.
- [x] 1.3 Implement the activation edge: keep the previous `active` in a `useRef` initialised to `false`, so the hook fires both on first mount with `active === true` (the "open a table" path) and on a later `false → true` transition. Do nothing on `true → false` and nothing while `active` stays `true`.
- [x] 1.4 Schedule the focus in a `requestAnimationFrame` so the grid root exists post-paint; store the handle and `cancelAnimationFrame` in the effect cleanup so a fast tab switch can't land a stale focus.
- [x] 1.5 Re-check the guards **inside** the RAF callback (not before scheduling): bail if `rootRef.current?.contains(document.activeElement)`, or if `isTextEntryTarget(document.activeElement)`. Then call `targetRef.current?.focus()` and **verify**: if focus did not land inside `rootRef`, call `rootRef.current?.focus()`. Verify rather than null-check `targetRef.current` — Postgres and MSSQL keep the grid mounted under `display: none` on non-Data subtabs, where `focus()` is a silent no-op.
- [x] 1.6 Write `packages/app/src/platform/shell/useAutoFocusOnActivate.test.tsx` modelled on `useSaveShortcut.test.tsx` (`renderHook` + hand-built DOM, `await` a frame before asserting). Cover: focuses target on mount-when-active; focuses on `false → true` rerender; falls back to root when `targetRef.current` is null; **falls back to root when the target's `focus()` is a no-op** (the hidden-grid case — simulate with a stub, since jsdom does no layout and will happily focus a `display: none` node); no-ops when focus is already inside root; no-ops when an `<input>` / `<textarea>` / `.cm-editor` node is focused; no-ops on `true → false`; does not re-fire on a rerender with `active` still `true`; cancels a pending frame on unmount.

## 2. Give each DataGrid a focusable handle

- [x] 2.1 In `packages/app/src/modules/postgres/data/DataGrid.tsx`: add `const rootRef = useRef<HTMLDivElement | null>(null)` and attach it to the `tabIndex={0}` root div (`:492-496`). Replace the `viewportRef.current?.parentElement` lookup at `:443-445` with `rootRef.current?.focus()` so the click path and the new programmatic path hit provably the same element.
- [x] 2.2 In the same file, add `focus(): void` to the `DataGridHandle` interface (`:100-103`) and to `useImperativeHandle` (`:211-228`), implemented as `rootRef.current?.focus()` (no-op when null).
- [x] 2.3 Repeat 2.1–2.2 in `packages/app/src/modules/mysql/data/DataGrid.tsx`, replacing the `e.currentTarget.closest("[tabindex]")` focus hack at `:535`.
- [x] 2.4 Repeat 2.1–2.2 in `packages/app/src/modules/mssql/data/DataGrid.tsx`, replacing the same hack at `:535`.
- [x] 2.5 Add `outline: none;` to `.root` in `packages/app/src/modules/{postgres,mysql,mssql}/data/DataGrid.module.css`, so opening a table via a keyboard path (⌘P) doesn't draw a UA `:focus-visible` ring around the whole grid while a mouse path doesn't. Do not add a replacement container-focus treatment — that's a separate design question.

## 3. Wire the viewers

- [x] 3.1 In `packages/app/src/modules/postgres/data/TableViewerTab.tsx`, call `useAutoFocusOnActivate({ active, rootRef, targetRef: gridRef })` next to the existing `useSaveShortcut(...)` at `:628`. `rootRef` (`:533`) and `gridRef` (`:538`) already exist; no new refs needed.
- [x] 3.2 Same wiring in `packages/app/src/modules/mysql/data/TableViewerTab.tsx` (`rootRef:197`, `gridRef:196`, `useSaveShortcut` at `:404`).
- [x] 3.3 Same wiring in `packages/app/src/modules/mssql/data/TableViewerTab.tsx` (`rootRef:242`, `gridRef:241`, `useSaveShortcut` at `:446`).
- [x] 3.4 Confirm the fallback path really engages. **Findings (corrected the original assumption):** the three engines hide the grid three different ways — MySQL genuinely unmounts it on a non-Data subtab *and* during loading/error (`TableViewerTab.tsx:657`); MSSQL unmounts only during loading/error (`:821-823`) and otherwise keeps it mounted under an inline `display: none` (`:507`); Postgres keeps it mounted under `.dataSubtab[data-active="false"] { display: none }` and unmounts it only while `isFirstLoad` (`:769`) — notably the `error` status still renders the grid. So `gridRef.current` is often non-null while the grid is unfocusable, which is why the hook verifies observed focus instead of null-checking the ref (task 1.5). Verified: no `null` deref (both focus calls are optional-chained), and the tab root carries `tabIndex={-1}` in all three (`postgres:705`, `mysql:419`, `mssql:461`).

## 4. Tests

- [x] 4.1 In `packages/app/src/modules/postgres/data/TableViewerTab.test.tsx`, add a focus block: render with `active` (defaults to `true` in `renderViewer`), await a frame, assert `document.activeElement` is the grid root; assert `⌘F` then opens the filter bar and focuses its first value input **without any intervening click** (the issue's exact repro). Mock `@tanstack/react-virtual` per the `DataGrid.copy.test.tsx:29-46` pattern if a real grid is rendered.
- [x] 4.2 Add a no-steal test: focus a detached `<input>` before the tab activates, await a frame, assert `document.activeElement` is still that input.
- [x] 4.3 Add a quick-switcher regression test: with an element standing in for the palette's `autoFocus` input focused, assert the viewer does not pull focus out of it while it is focused — and that focus lands in the grid once that input is blurred/removed and the tab activates.
- [x] 4.4 Add a non-destructive test: after auto-focus, assert no active cell and no row-range selection are set (no cell carries the active-cell class, no row is selected).
- [x] 4.5 Add equivalent activation-focus tests to `packages/app/src/modules/mysql/data/__tests__/TableViewerTab.test.tsx` and `packages/app/src/modules/mssql/data/__tests__/TableViewerTab.test.tsx`, asserting ⌘R reaches the root `onKeyDown` handler without a priming click. **Keep the existing mock discipline**: `DataGrid` / `FilterBar` / `Inspector` mocks must return stable object references or the suite infinite-loops and exhausts the heap (see the header comment at `mysql/.../TableViewerTab.test.tsx:10-16`); the mocked `DataGrid` needs a `focus()` on its forwarded handle.

## 5. Verify

- [x] 5.1 `pnpm -C packages/app typecheck` — clean.
- [x] 5.2 `pnpm -C packages/app test:run` — 132 files / 1662 tests pass, including every pre-existing `DataGrid.*.test.tsx` suite touched by the `rootRef` refactor. **Two files fail (47 tests): `modules/ai/components/ChatPanel.test.tsx` and `platform/connection-registry/useExpandedGroups.test.ts` — both `localStorage` environment errors, both unrelated to this change.** Verified pre-existing: stashing this change and re-running those two files reproduces the identical 47 failures on a clean tree.
- [x] 5.3 `pnpm -C packages/app lint` — 0 errors. 91 repo-wide pre-existing warnings; the 8 files touched by this change contribute none (verified by linting them on a stashed clean tree and getting the identical 4 warnings, shifted only by the added import line).
- [ ] 5.4 Manual QA against a real Postgres connection — the issue's repro: open a table from the schema tree, press ⌘F with no click → filter bar opens and its first value input is focused; press ⌘R → table reloads; press ⌘1/⌘2/⌘3 → subtabs switch. Repeat opening via ⌘P quick switcher. Repeat for MySQL and MSSQL (⌘R / ⌘Z only — those engines have no ⌘F).
- [ ] 5.5 Manual QA of the no-steal cases: type in the connection search box and in the SQL editor (CodeMirror) while a table tab is open in the background → focus and caret unchanged; open the command palette → its input keeps focus.
- [ ] 5.6 Manual QA of appearance: open a table via ⌘P and by clicking the tree — no focus ring is drawn around the grid container in either case, and the two look identical.
- [ ] 5.7 Note for the reviewer (do not fix here): ⌘R now reaches both `WorkspaceShell`'s tree-refresh binding (`:400-411`) and the viewer's reload — the same double-fire that already occurs today once the user clicks into the grid.
- [ ] 5.8 Manual QA of the subtab focus-loss path (see design Open Questions): open a table, press ⌘2 (Structure) with no click, then press ⌘1. If ⌘1 does nothing, focus was reset to `document.body` when the grid was hidden — confirm in a real browser and file the follow-up (relax the tab-root guard to accept `body`/`null` like `useSaveShortcut` does). Not fixable by test: jsdom does no layout.
