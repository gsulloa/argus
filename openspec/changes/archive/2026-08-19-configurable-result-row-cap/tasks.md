## 1. Shared backend primitives

- [x] 1.1 Create `packages/app/src-tauri/src/platform/row_cap.rs`: `pub const DEFAULT_ROW_CAP: u64 = 10_000`, `pub const HARD_ROW_CAP: u64 = 1_000_000`, `pub const SETTING_KEY: &str = "sql.rowCap"`, plus `#[derive(Serialize)] pub enum RowCapSource { Setting, Statement, Engine }` with `#[serde(rename_all = "snake_case")]`. Register the module in `platform/mod.rs`.
- [x] 1.2 Add `pub fn configured_cap(conn: &rusqlite::Connection) -> u64` — reads `settings::get(conn, SETTING_KEY)`, parses `u64`, `unwrap_or(DEFAULT_ROW_CAP)`, then `.clamp(1, HARD_ROW_CAP)`. Model it on `read_history_retention` (`lib.rs:120-137`); a missing/garbage value must never error.
- [x] 1.3 Add `pub fn effective_cap(configured: u64, explicit: Option<u64>, engine_ceiling: u64) -> (u64, RowCapSource)` implementing `min(engine_ceiling, max(configured, explicit.unwrap_or(0)))` and returning the source: `Engine` when an engine ceiling below `HARD_ROW_CAP` clamped it, `HardCeiling` when `HARD_ROW_CAP` is binding, else `Setting`.
- [x] 1.4 Unit-test `configured_cap` (absent key, `""`, `"abc"`, `"0"`, `"5000000"`, `"50000"`) and `effective_cap` (all six source/clamp combinations incl. the CloudWatch `engine_ceiling = 10_000` case).

## 2. Explicit-limit detector

- [x] 2.1 Create `packages/app/src-tauri/src/platform/sql_limit.rs` with `pub enum Dialect { Postgres, MySql, TSql, Presto }` and `pub fn explicit_row_limit(sql: &str, dialect: Dialect) -> Option<u64>`. Build the byte scanner by adapting the quoting/comment state machines already in `mysql/sql.rs:382` (`split_statements`) and `mssql/sql.rs:339`: `'…'` with `\'`/`''`, `"…"`, `` `…` ``, `[…]` (T-SQL), `$tag$…$tag$` (Postgres), `--`/`#` line comments, `/* */` blocks, and a parenthesis-depth counter. No new crate dependency.
- [x] 2.2 Implement the leading-keyword gate: skip leading comments/whitespace (reuse `skip_leading_comments_tsql` at `mssql/sql.rs:22` and the equivalent pg/mysql skippers), and return `None` unless the first significant keyword is `SELECT` / `WITH` / `TABLE` / `VALUES` / `SHOW`.
- [x] 2.3 Implement `Dialect::Postgres` / `Presto`: trailing depth-0 `LIMIT <int>` (optionally followed by `OFFSET <int>`), plus `FETCH FIRST|NEXT <int> ROW|ROWS ONLY` for Postgres. `LIMIT ALL` / `LIMIT NULL` / non-integer → `None`.
- [x] 2.4 Implement `Dialect::MySql`: trailing depth-0 `LIMIT <int>` and `LIMIT <offset>, <count>` → `Some(count)`.
- [x] 2.5 Implement `Dialect::TSql`: leading `SELECT [ALL|DISTINCT] TOP (<int>)` / `TOP <int>` (reject `PERCENT`), and trailing depth-0 `OFFSET <int> ROWS FETCH NEXT <int> ROWS ONLY` → the `FETCH NEXT` value.
- [x] 2.6 Write the adversarial `None` corpus as unit tests — every one of these must yield `None`: `SELECT 'limit 30000' AS note FROM t`, `SELECT * FROM t -- LIMIT 99999`, `/* LIMIT 1 */ SELECT * FROM t`, `SELECT * FROM t WHERE id IN (SELECT id FROM u LIMIT 5)`, a CTE with `LIMIT` inside it, `SELECT $$ LIMIT 42 $$`, `LIMIT ALL`, `LIMIT NULL`, `LIMIT $1`, `LIMIT ?`, `LIMIT @n`, `LIMIT 1+1`, `SELECT TOP 50 PERCENT * FROM t`, `INSERT INTO … LIMIT 5`, `` SELECT `limit 5` FROM t ``, `SELECT [LIMIT 5] FROM t`.
- [x] 2.7 Write the positive corpus as unit tests, one per dialect row of the spec table, including `LIMIT 250 OFFSET 1000`, `LIMIT 1000, 250`, `TOP (25000)`, `OFFSET 0 ROWS FETCH NEXT 20000 ROWS ONLY`, mixed case, trailing `;`, and trailing whitespace/newline.

## 3. Postgres backend (reference engine)

- [x] 3.1 Delete `const RESULT_ROW_CAP` at `postgres/sql.rs:46`. Thread a `cap: u64` parameter into `run_one` and `run_one_stream`; resolve it at each command entry (`postgres_run_sql` `:1022`, `postgres_run_sql_stream` `:1179`, `postgres_run_sql_many` `:1339`) from `app.state::<DbState>()` + `row_cap::configured_cap` + `explicit_row_limit(sql, Dialect::Postgres)` + `effective_cap(…, HARD_ROW_CAP)`. Resolve per statement inside the `run_many` loop, not once per run.
- [x] 3.2 Rewrite `run_one` (`postgres/sql.rs:702-736`) onto `client.query_raw(&stmt, empty_iter)` + `futures::pin_mut!` + `try_next()`, mirroring `run_one_stream` (`:794-833`), fetching up to `row_cap::fetch_budget(cap)` rows, returning at most `cap` of them, and setting `truncated = (fetched > cap)`. Delete the stale "materializes the entire result set" comment at `:713-715`.
- [x] 3.3 Add `row_cap: u64` and `row_cap_source: RowCapSource` to `RunSqlResult::Rows` (`:159-174`) and to `StreamEvent::Done` (`:216-227`); populate them in both paths.
- [x] 3.4 Verify the activity-log metric and `query_history` row count both derive from the actual returned row count (so a raised cap or an honoured `LIMIT` reports the real number).
- [x] 3.5 Rust tests: cap resolution per statement in a multi-statement run; `LIMIT 30000` above a 10,000 setting yields cap 30,000; `LIMIT 5000000` clamps to `HARD_ROW_CAP` with source `HardCeiling`; subquery `LIMIT` does not raise the cap. `cargo test` + `cargo check`.

## 4. Remaining engine backends

- [x] 4.1 **MySQL** (`mysql/sql.rs`): delete the const at `:39`; thread `cap` into `run_single_sql_inner`; resolve with `Dialect::MySql` at `mysql_run_sql` (`:894`) and per statement in `mysql_run_sql_many` (`:1005`); at `:742-757`, take at most `cap` rows and set `truncated = (fetched > cap)` using `row_cap::fetch_budget(cap)` as the fetch/slice boundary; add `row_cap`/`row_cap_source` to `RunSqlResult::Rows` (`:49-63`).
- [x] 4.2 **MySQL**: change `truncated: truncated || any_truncated` (`:756`) to `truncated` alone, leaving `any_truncated` to flow only through `truncated_columns`.
- [x] 4.3 **MSSQL** (`mssql/sql.rs`): same as 4.1 with `Dialect::TSql` — const at `:39`, slice at `:806-822`, struct at `:218-240`, commands `mssql_run_sql` (`:939`) / `mssql_run_sql_many` (`:1072`); resolve per statement per `Batch`/`Statement` from `split_statements`.
- [x] 4.4 **MSSQL**: change `truncated: result_truncated || any_truncated` (`:821`) to `result_truncated` alone.
- [x] 4.5 **Athena** (`athena/sql.rs`): delete the const at `:25`; add a `cap: usize` parameter to `process_page_rows` (`:361-369`) and use it at `:385`; accumulate up to `row_cap::fetch_budget(cap)` rows, return at most `cap`, and set `truncated = (accumulated > cap)`; the `while !truncated && next_token.is_some()` guard at `:325` already stops pagination once that flag is set. Resolve with `Dialect::Presto` at `athena_run_sql` (`:404`) and per statement in `athena_run_sql_many` (`:471`, including its single-statement re-split fallback at `:485-492`). Add `row_cap`/`row_cap_source` to `RunSqlResult::Rows` (`:44-66`). Update the existing test `result_row_cap_sets_truncated_flag` (`:750-772`) for the parameterised cap and the new `fetched > cap` semantics.
- [x] 4.6 **Dynamo** (`dynamo/partiql.rs`): delete the const at `:183`; thread the configured cap into the item loop at `:225`, accumulating up to `row_cap::fetch_budget(cap)` items, returning at most `cap`, and setting `truncated = (accumulated > cap)`; **no** statement-limit detection (PartiQL has no `LIMIT` clause) so `row_cap_source` is always `Setting`. Add `row_cap`/`row_cap_source` to `RunPartiQLResult::Rows` (`:106-121`); commands `dynamo_run_partiql` (`:276`) / `dynamo_run_partiql_many` (`:365`).
- [x] 4.7 **CloudWatch** (`cloudwatch/insights.rs`): delete the const at `:27`; compute `effective = effective_cap(configured, None, 10_000)`; use it in the projection loop at `:279`, accumulating up to `row_cap::fetch_budget(effective)` records, projecting at most `effective`, and setting `truncated = (accumulated > effective)`; use `min(fetch_budget(effective), 10_000)` in the `.clamp(1, …)` at `:365` (so the server-side `limit` param carries the extra probe record too), leaving the `has_limit_command` branch at `:362-366` intact. Add `row_cap`/`row_cap_source` to `InsightsResult::Rows` (`:53-65`) — source is `Engine` when `configured > 10_000`, else `Setting`.
- [x] 4.8 Confirm no `RESULT_ROW_CAP` reference survives (`rg RESULT_ROW_CAP src-tauri`), then `cargo test` + `cargo clippy` clean.

## 5. Shared frontend primitives

- [x] 5.1 Create `packages/app/src/platform/sql/useRowCap.ts` wrapping `useSetting<number>("sql.rowCap", 10000)`, exporting `DEFAULT_ROW_CAP` / `HARD_ROW_CAP` constants that mirror the Rust ones, plus a `clampRowCap(n)` validator for `[1, 1_000_000]`.
- [x] 5.2 Create `platform/sql/RowCapSelector.tsx` (+ `.module.css`): compact `Limit: 10k ▾` trigger, presets `1k / 10k / 50k / 100k`, `Custom…` numeric entry rejecting out-of-range values without writing. Reuse the existing Radix menu primitives; style per `DESIGN.md` (read it first).
- [x] 5.3 Create `platform/sql/TruncationBanner.tsx` (+ `.module.css`) taking `{ rowCap, rowCapSource, clause?: string | null, onRaiseLimit?: () => void }`. Render the three copy variants from the `sql-result-row-cap` spec, thousands-separated via `toLocaleString()`; show **Raise limit** only for `source === "setting"`. Use `var(--warning)` — not the hardcoded `rgba(245,158,11,0.1)` copied into five files today — hairline border, compact density, no gradient.
- [x] 5.4 Tests for `TruncationBanner`: each `row_cap_source` variant's copy, `100000 → "100,000"` formatting, Raise-limit shown/hidden, Dynamo's clause-less variant. Tests for `RowCapSelector`: preset write, custom accept/reject, display sync after an external change.

## 6. Frontend wiring per engine

- [x] 6.1 Add `row_cap: number` and `row_cap_source: "setting" | "statement" | "engine"` to the six result types: `postgres/sql/api.ts:5-42` (both `RunSqlResult` and the `done` `StreamEvent`), `mysql/types.ts:325`, `mssql/types.ts:345`, `athena/types.ts:78`, `dynamo/sql/api.ts:17`, `cloudwatch/types.ts:81`. Fix every `tsc` construction site the additions surface, test fixtures included (e.g. `makeRunState()` in `postgres/sql/ResultPanel.test.tsx:57-80`).
- [x] 6.2 **Postgres** `sql/ResultPanel.tsx`: replace the inline banner at `:200-204` with `<TruncationBanner />`; stop passing `truncated={false}` on the streaming branch at `:37` and drive it from the `done` event's flag; thread `row_cap`/`row_cap_source` through `useQueryRun.ts:262-269`.
- [x] 6.3 Replace the inline banners in the other five panels with `<TruncationBanner />`: `mysql/sql/ResultPanel.tsx:201-214`, `mssql/sql/ResultPanel.tsx:197-210`, `athena/sql/ResultPanel.tsx:169-182`, `dynamo/sql/ResultPanel.tsx:227-240`, `cloudwatch/insights/ResultPanel.tsx:127-140`. Pass the right `clause` per engine (`LIMIT`, `TOP / OFFSET … FETCH NEXT`, `null` for Dynamo).
- [x] 6.4 Add `<RowCapSelector />` to the six editor toolbars (next to Run), wired to `useRowCap`; hook the banners' **Raise limit** action to the same menu.
- [x] 6.5 Fix the Athena export gap: pass `truncated={…}` to `<ExportMenu />` at `athena/sql/ResultPanel.tsx:207-213` so truncated exports get the `_truncated` suffix.
- [x] 6.6 Confirm the `(truncated)` summary suffix and the `+` multi-statement tab marker still read correctly under a raised cap (`*/sql/useQueryRun.ts` `summarize()`, `MultiStatementTabs.tsx`); no hardcoded `10,000` remains anywhere in `packages/app/src` (`rg "10,000" src`).

## 7. Verify

- [x] 7.1 `cargo test` in `packages/app/src-tauri`, and `cargo clippy --lib` introducing no new warnings (the crate carries 91 pre-existing warnings unrelated to this change; `-D warnings` is not achievable without a separate repo-wide cleanup).
- [x] 7.2 `pnpm -C packages/app typecheck`, `pnpm -C packages/app lint`, `pnpm -C packages/app test:run`.
- [ ] 7.3 Manual QA against a Postgres table with > 100k rows: `SELECT * FROM t LIMIT 30000` returns 30,000 rows with no banner (the issue's exact repro); `SELECT * FROM t` truncates at the setting and shows the banner naming that number; raising the limit from the banner and re-running returns more rows.
- [ ] 7.4 Manual QA of the memory guard: `LIMIT 5000000` stops at 1,000,000 with the hard-ceiling banner and no **Raise limit** action; watch RSS during the run to confirm the Postgres streaming rewrite bounds it (feeds the design's open question on the ceiling value).
- [ ] 7.5 Manual QA per engine: MySQL `LIMIT 30000`; MSSQL `TOP (30000)` and `FETCH NEXT 25000 ROWS ONLY`; Athena `LIMIT 30000`; Dynamo PartiQL with `sql.rowCap` raised to 50k; CloudWatch with `sql.rowCap` at 100k (must stay at 10,000 with the engine-reason banner) and at 500 (must clamp down).
- [ ] 7.6 MySQL/MSSQL regression check for D5: a 3-row result with a > 1 MiB `TEXT` cell shows **no** truncation banner, exports without the `_truncated` suffix, and still reports the column in `truncated_columns`.
- [x] 7.7 Update `CHANGELOG.md` and the README's SQL-editor notes with the new `sql.rowCap` setting, the honoured-`LIMIT` behaviour, and the 1,000,000-row ceiling.
